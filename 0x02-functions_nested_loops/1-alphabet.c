#include <stdio.h>
#include "main.h"
/**
 * main - print alphabet in lower case
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = '0'; n <= 'z'; n++)
	{
		putchar('n');
	}
	putchar('\n');
	return (0);
}
